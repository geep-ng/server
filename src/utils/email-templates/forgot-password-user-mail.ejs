<!DOCTYPE html>
<html>
  <head>
    <title>LUJ Password Reset</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style type="text/css">
      /* Base */
      body {
        margin: 0;
        padding: 0;
        min-width: 100%;
        font-family: Arial, sans-serif;
        font-size: 16px;
        line-height: 1.5;
        background-color: #f0f0f0; /* Lighter background for better contrast */
        color: #333333; /* Darker text for readability */
      }
      a {
        color: #f87171; /* Brand primary color for links */
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
      h1 {
        font-size: 28px; /* Slightly larger for emphasis */
        font-weight: 700;
        line-height: 1.25;
        margin-top: 0;
        margin-bottom: 20px;
        text-align: center;
        color: #333333; /* Consistent with body text */
      }
      h2 {
        font-size: 36px; /* Prominent for OTP */
        font-weight: 700;
        color: #f87171; /* Brand primary color for OTP */
        margin-top: 20px;
        margin-bottom: 30px;
        text-align: center;
        letter-spacing: 2px; /* Spacing for better readability of the code */
      }
      p {
        margin-top: 0;
        margin-bottom: 20px; /* Adjusted spacing */
      }
      table td {
        vertical-align: top;
      }
      /* Layout */
      .email-wrapper {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.05); /* Subtle shadow for depth */
        border-radius: 8px; /* Slightly rounded corners */
        overflow: hidden; /* Ensures content stays within rounded corners */
      }
      .email-header {
        background-color: #f87171; /* Brand primary color for header */
        padding: 30px 24px; /* More padding */
        color: #ffffff;
        text-align: center;
      }
      .email-header h1 {
        color: #ffffff; /* White text for header title */
        margin-bottom: 0;
      }
      .email-body {
        padding: 30px 40px; /* Increased horizontal padding */
        background-color: #ffffff;
        text-align: center; /* Center align body text for a cleaner look */
      }
      .email-footer {
        background-color: #333333; /* Black for a modern, strong footer */
        padding: 24px;
        color: #ffffff;
        font-size: 14px;
        text-align: center;
        border-bottom-left-radius: 8px; /* Match wrapper border radius */
        border-bottom-right-radius: 8px; /* Match wrapper border radius */
      }
      .email-footer p {
        margin-bottom: 8px; /* Smaller margin for footer text */
      }
      .email-footer a {
        color: #f87171; /* Brand primary color for footer links */
        text-decoration: none;
      }
      /* Buttons (if needed, though not directly used in this template) */
      .button {
        display: inline-block;
        background-color: #f87171; /* Brand primary color for buttons */
        color: #ffffff;
        font-size: 16px;
        font-weight: 700;
        text-align: center;
        text-decoration: none;
        padding: 12px 24px;
        border-radius: 4px;
        transition: background-color 0.3s ease; /* Smooth hover effect */
      }
      .button:hover {
        background-color: #e55a5a; /* Slightly darker shade on hover */
      }
    </style>
  </head>
  <body>
    <div class="email-wrapper">
      <div class="email-header">
        <h1>LUJ Password Reset</h1>
      </div>
      <div class="email-body">
        <p>Hello <%= name %>,</p>
        <p>
          We received a request to reset your password for your LUJ account.
          If this was you, please use the OTP below to proceed with your password reset:
        </p>
        <h2><%= otp %></h2>
        <p>
          This OTP is valid for the next <strong>5 minutes</strong>. For your security, please use it promptly.
        </p>
        <p style="font-size: 14px; color: #777777;">
          If you did not request a password reset, please ignore this email. Your account remains secure.
        </p>
      </div>
      <div class="email-footer">
        <p>
          Need help? Contact our support team at
          <a href="mailto:hello@luj.ng">hello@luj.ng</a>
        </p>
        <p>&copy; <%= new Date().getFullYear() %> LUJ. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
